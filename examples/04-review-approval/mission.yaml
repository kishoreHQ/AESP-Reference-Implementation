apiVersion: aesp.mission/v1
kind: Mission
metadata:
  id: example.04-review-approval
  profilePortable: true
spec:
  goal: "Review/approval workflow"
  requiredCapabilities:
    - coding
  successCriteria:
    - example-complete
  budget:
    maxSteps: 20
    maxTokens: 50000
    maxWallSec: 600
  constraints:
    - capability-routing-only
    - no-vendor-model-names
