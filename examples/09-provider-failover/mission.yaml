apiVersion: aesp.mission/v1
kind: Mission
metadata:
  id: example.09-provider-failover
  profilePortable: true
spec:
  goal: "Provider-failover workflow"
  requiredCapabilities:
    - coding
    - tools
  successCriteria:
    - example-complete
  budget:
    maxSteps: 20
    maxTokens: 50000
    maxWallSec: 600
  constraints:
    - capability-routing-only
    - no-vendor-model-names
